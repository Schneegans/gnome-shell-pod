[Unit]
Description=Run nested Wayland gnome session for display %I

[Service]
Environment="DISPLAY=%I"
Environment="MUTTER_DEBUG_DUMMY_MODE_SPECS=1920x1080@60.0"
ExecStart=gnome-shell --nested --wayland

[Install]
WantedBy=default.target
